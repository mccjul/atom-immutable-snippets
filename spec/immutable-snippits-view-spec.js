'use babel';

import ImmutableSnippitsView from '../lib/immutable-snippits-view';

describe('ImmutableSnippitsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
